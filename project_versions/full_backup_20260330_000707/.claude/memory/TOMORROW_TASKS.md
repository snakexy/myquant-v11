# 明天待办事项 - 2026-03-27

## 🔴 高优先级

### 1. HotDB 聚合时间戳错误修复
**问题**：14:25 显示为1小时K线最后一条（应该是15:00）
**根因**：`hotdb_adapter.py:875` 聚合时取 `datetimes_2d[:, 0]`（第一个时间）而非最后一个
**影响**：1h/30m/15m K线时间轴显示错误

**修复方案**：
```python
# hotdb_adapter.py 第875行
# 错误：agg_datetime = datetimes_2d[:, 0]
# 正确：agg_datetime = datetimes_2d[:, -1]  # 取最后一个时间
```

**验证**：
- 1h: 应该是 14:00, 15:00（整点）
- 30m: 应该是 14:00, 14:30, 15:00
- 15m: 应该是 14:15, 14:30, 14:45, 15:00

---

### 2. 调度器休市降频优化
**问题**：调度器在休市时仍每3秒执行，浪费资源
**位置**：`frontend/src/utils/refreshScheduler.ts`

**修复方案**：
- 检测市场状态（`/api/v1/quotes/market/status`）
- 休市时：降低频率（3秒 → 30秒）
- 开盘时：恢复正常高频（3-5秒）

---

## 🟡 中优先级

### 3. 清理旧的错误数据
**问题**：HotDB中存储了错误时间戳的聚合数据
**解决**：用户删除自选已实现HotDB数据清理
**待验证**：删除后重新添加是否获取正确数据

---

## 📋 相关文件

| 文件 | 修改内容 |
|------|----------|
| `backend/src/myquant/core/market/adapters/hotdb_adapter.py:875` | 修复聚合时间戳（取最后一个） |
| `frontend/src/utils/refreshScheduler.ts` | 添加市场状态检测，休市降频 |

---

## ⚠️ 注意事项

1. 修改聚合逻辑后，需要**清除HotDB缓存**
2. 修改调度器前，先测试市场状态API
3. 修复后验证所有周期的K线时间轴

---

**今天已完成**：
- ✅ data_source 返回真实数据源名称
- ✅ 删除自选时清理HotDB数据
- ✅ 减少调度器日志刷屏
