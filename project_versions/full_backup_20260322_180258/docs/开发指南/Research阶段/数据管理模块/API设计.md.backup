# 数据管理模块 - API设计

> **阶段**: Research阶段
> **模块**: 数据管理
> **状态**: ✅ 已实现
> **版本**: v3.0 (实际实现版本)
> **最后更新**: 2026-02-11

> **说明**: 本文档反映实际代码实现，与实施记录.md同步

---

## 🎯 模块定位

数据管理模块是Research阶段的基础设施，负责：
- 多层缓存管理（L1/L2/L3）
- 数据源配置与存储
- 数据刷新与更新
- 缓存状态监控
- 指数成分股管理

---

## 📡 API端点列表

### 1. 刷新数据

**端点**: `POST /api/v1/research/data/refresh`

**描述**: 刷新股票数据，清除缓存后重新获取

**请求参数**:
```json
{
  "instruments": ["000001.SZ", "000002.SZ"],  // 可选，股票列表
  "start_date": "2020-01-01",                 // 可选，开始日期
  "end_date": "2024-12-31",                   // 可选，结束日期
  "clear_cache": true                         // 可选，是否清除缓存
}
```

**响应**:
```json
{
  "code": 200,
  "data": {
    "task_id": "refresh_20240210_123456",
    "status": "running",
    "total": 100,
    "completed": 20,
    "message": "正在刷新数据..."
  },
  "message": "success"
}
```

**错误响应**:
```json
{
  "code": 400,
  "data": null,
  "message": "日期格式错误，应为 YYYY-MM-DD"
}
```

---

### 2. 缓存统计

**端点**: `GET /api/v1/research/data/cache-stats`

**描述**: 获取多层缓存统计信息

**请求参数**: 无

**响应**:
```json
{
  "code": 200,
  "data": {
    "L1": {
      "name": "内存缓存",
      "hit_rate": 0.85,
      "size": "256 MB",
      "count": 1523
    },
    "L2": {
      "name": "磁盘缓存",
      "hit_rate": 0.12,
      "size": "2.5 GB",
      "count": 12456
    },
    "L3": {
      "name": "数据库",
      "hit_rate": 0.03,
      "size": "15.2 GB",
      "count": 152340
    },
    "overall_hit_rate": 0.97
  },
  "message": "success"
}
```

---

### 3. 数据配置

**端点**: `POST /api/v1/research/data/config`

**描述**: 保存数据源配置

**请求参数**:
```json
{
  "provider": "tushare",           // 数据源提供商
  "api_token": "your_token_here",  // API密钥
  "priority": 1,                   // 优先级
  "enabled": true                  // 是否启用
}
```

**响应**:
```json
{
  "code": 200,
  "data": {
    "config_id": "cfg_tushare_001",
    "message": "配置已保存"
  },
  "message": "success"
}
```

---

### 4. 获取数据配置

**端点**: `GET /api/v1/research/data/config`

**描述**: 获取当前数据源配置

**请求参数**: 无

**响应**:
```json
{
  "code": 200,
  "data": {
    "providers": [
      {
        "name": "tushare",
        "enabled": true,
        "priority": 1,
        "last_update": "2024-02-10 10:30:00"
      },
      {
        "name": "akshare",
        "enabled": true,
        "priority": 2,
        "last_update": "2024-02-10 10:25:00"
      }
    ]
  },
  "message": "success"
}
```

---

### 5. 获取指数成分股

**端点**: `GET /api/v1/research/data/index/{id}`

**描述**: 获取指数成分股列表

**路径参数**:
- `id`: 指数代码（如 csi300, csi500, sse50）

**查询参数**:
- `date`: 可选，查询日期（默认最新）

**响应**:
```json
{
  "code": 200,
  "data": {
    "index_id": "csi300",
    "date": "2024-02-10",
    "stocks": [
      "000001.SZ",
      "000002.SZ",
      "600000.SH"
    ],
    "total": 300
  },
  "message": "success"
}
```

**错误响应**:
```json
{
  "code": 404,
  "data": null,
  "message": "未找到指数 csi300 的成分股数据"
}
```

---

## 🔒 错误处理规范

### 错误码定义

| 错误码 | 说明 | 示例 |
|--------|------|------|
| 200 | 成功 | 请求成功 |
| 400 | 参数错误 | 日期格式错误 |
| 401 | 未认证 | API密钥无效 |
| 404 | 资源不存在 | 指数代码不存在 |
| 500 | 服务器内部错误 | 数据库连接失败 |

### 统一错误响应格式

```json
{
  "code": 400,
  "data": null,
  "message": "详细的错误描述信息"
}
```

---

## 📝 使用示例

### Python示例

```python
import requests

# 1. 刷新数据
response = requests.post(
    "http://localhost:8000/api/v1/research/data/refresh",
    json={
        "instruments": ["000001.SZ", "000002.SZ"],
        "start_date": "2020-01-01",
        "end_date": "2024-12-31",
        "clear_cache": True
    }
)
print(response.json())

# 2. 获取缓存统计
response = requests.get("http://localhost:8000/api/v1/research/data/cache-stats")
stats = response.json()["data"]
print(f"整体命中率: {stats['overall_hit_rate']:.2%}")

# 3. 获取指数成分股
response = requests.get("http://localhost:8000/api/v1/research/data/index/csi300")
stocks = response.json()["data"]["stocks"]
print(f"CSI300成分股数量: {len(stocks)}")
```

### JavaScript示例

```javascript
// 刷新数据
const refreshData = async () => {
  const response = await fetch('/api/v1/research/data/refresh', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      instruments: ['000001.SZ', '000002.SZ'],
      clear_cache: true
    })
  });
  const result = await response.json();
  console.log(result);
};

// 获取缓存统计
const getCacheStats = async () => {
  const response = await fetch('/api/v1/research/data/cache-stats');
  const result = await response.json();
  console.log('缓存统计:', result.data);
};
```

---

## 🔗 相关文档

- [数据模型](./数据模型.md) - 数据表结构和字段定义
- [前端组件](./前端组件.md) - 前端UI组件文档
- [Research阶段README](../README.md) - 阶段概述
- [第7章 - 完整API参考](../../../项目设计/MyQuant完整架构与工作流V3/07-完整API参考.html) - 全部API列表

---

**维护说明**: 本文档与代码实现保持同步，如有API变更请及时更新
**最后更新**: 2026-02-10
