[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "myquant"
version = "11.0.0"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src", "external"]
