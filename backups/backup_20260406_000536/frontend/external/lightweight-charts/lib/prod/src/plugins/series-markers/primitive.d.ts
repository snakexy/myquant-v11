import { ISeriesPrimitive, SeriesAttachedParameter } from '../../api/iseries-primitive-api';
import { DeepPartial } from '../../helpers/strict-type-checks';
import { IPrimitivePaneView, PrimitiveHoveredItem } from '../../model/ipane-primitive';
import { AutoscaleInfo } from '../../model/series-options';
import { Logical } from '../../model/time-data';
import { SeriesMarkersOptions } from './options';
import { SeriesMarker } from './types';
export declare class SeriesMarkersPrimitive<HorzScaleItem> implements ISeriesPrimitive<HorzScaleItem> {
    private _paneView;
    private _markers;
    private _indexedMarkers;
    private _dataChangedHandler;
    private _series;
    private _chart;
    private _requestUpdate?;
    private _autoScaleMarginsInvalidated;
    private _autoScaleMargins;
    private _markersPositions;
    private _cachedBarSpacing;
    private _recalculationRequired;
    private _options;
    constructor(options: DeepPartial<SeriesMarkersOptions>);
    attached(param: SeriesAttachedParameter<HorzScaleItem>): void;
    requestUpdate(): void;
    detached(): void;
    setMarkers(markers: SeriesMarker<HorzScaleItem>[]): void;
    markers(): readonly SeriesMarker<HorzScaleItem>[];
    paneViews(): readonly IPrimitivePaneView[];
    updateAllViews(): void;
    hitTest(x: number, y: number): PrimitiveHoveredItem | null;
    autoscaleInfo(startTimePoint: Logical, endTimePoint: Logical): AutoscaleInfo | null;
    applyOptions(options: DeepPartial<SeriesMarkersOptions>): void;
    private _getAutoScaleMargins;
    private _getMarkerPositions;
    private _recalculateMarkers;
    private _updateAllViews;
    private _onDataChanged;
}
